# @package _global_
# Default logging configuration: INFO level, console + file handlers.
# Override at runtime: python -m lsl_bridge logging=debug
logging:
  level: INFO
  file: ./lsl_bridge.log
  format: "[%(asctime)s][%(name)s][%(levelname)s] - %(message)s"
