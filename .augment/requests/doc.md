Based on the contents of the Device_Application folder, and the contents of the Device_Calibration folder, I need you to fill the Handgrip/README.md file with a detailed architectural high-level description of each one of the projects, and a low level detailed explanation of the SPI communication protocol to communicate with the device. 

The Device_Application/Firmware folder contains the Arduino code for the device application. The Device_Calibration folder contains the Arduino code for the device calibration, that's expected to be used in parallel as the python script at Device_Calibration/Scripts/read_calibration_data.py, reads the data from the device and saves it in a file. After that data is saved, the python script at Device_Calibration/Scripts/apply_linear-regression.py applies a linear regression to the data in order to get the intercept and gradient of the calibration curve.

So, for the task, is mandatory that you take the time to identify the semantic purpose of low-level purpose of each one of the 'magic numbers' in the code, and to explain the SPI communication protocol to communicate with the device.

Also, for the Application Firmware architecture, the expected input is a time-series of data generated by the HX711 sensor, as the user is pressing on the handgrip, and the expected output is at formated & processed data representative of the user's grip exercise. It's expected that you map from the input data to the output data, and that you explain the mapping in the README.md file.

For the Calibration Firmware architecture, the expected input is a time-series of data generated by the HX711 sensor, as the user is pressing on the handgrip, and the expected output is a real time calibration curve, that's expected to be used in parallel as the python script at Device_Calibration/Scripts/read_calibration_data.py, reads the data from the device and saves it in a file. It's expected that you map from the input data to the output data, and that you explain the mapping in the README.md file.



-------------------

Create comprehensive technical documentation for `Handgrip/README.md` covering both the Device_Application and Device_Calibration projects. The documentation must include:

## Epistemic Quality Requirements
  - Highest level as possible (Known (based on evidence, manual, reference documents, etc.))
  - No hallucinations or false assumptions
  - Any claim done in the comments needs to be supported by evidence
  - Review the binnacle.md for guidance on my mental process
  - Make sure to review the links attached in the code and in the binnacle.md in order to validate the claims on each one. 

## 1. High-Level Architecture Descriptions

### Device_Application Architecture
- **Purpose**: Real-time grip exercise monitoring and event detection
- **Input**: Time-series data from HX711 load cell sensor (24-bit signed integers via SPI => confirm this information on the web) 
- **Output**: Formatted grip exercise data including:
  - Start/end event markers
  - Peak force detection
  - Calibrated force values in kilograms
  - Point-by-point time-series during active grip
- **Data Flow Mapping**: Document the complete transformation pipeline from raw HX711 readings to processed exercise metrics, including:
  - Calibration offset application
  - Derivative-based event detection (start/end of grip)
  - Peak tracking algorithm
  - Force scaling using linear regression parameters

### Device_Calibration Architecture
- **Purpose**: Generate calibration parameters (intercept and gradient) for force-to-kilogram conversion
- **Input**: Time-series data from HX711 sensor during known reference weight applications
- **Output**: Real-time sensor readings transmitted via serial for offline calibration curve fitting
- **Workflow**: Two-stage process:
  1. Arduino firmware (`Device_Calibration/Firmware/write_calibration_data.ino`) streams raw sensor data
  2. Python script (`Device_Calibration/Scripts/read_calibration_data.py`) collects data with user-provided reference weights
  3. Python script (`Device_Calibration/Scripts/apply_linear-regression.py`) computes linear regression to derive `gradient` and `intercept`
- **Data Flow Mapping**: Document how raw sensor values are collected, associated with reference weights, and processed into calibration coefficients

## 2. SPI Communication Protocol (Low-Level Detail)

Provide a detailed technical explanation of the SPI interface between the Arduino (slave) and HX711 sensor (master):

### Hardware Configuration
- Pin mappings (MOSI/DOUT, MISO, SCK/PD_SCK, SS)
- Explain the role of `SSpin` (pin 9) as slave select indicator

### SPI Register Configuration (SPCR)
Decode each "magic number" bit manipulation operation in `SPI_config()`:
- `0x80` (SPIE): SPI interrupt enable
- `0x40` (SPE): SPI enable
- `0xDF` (DORD): MSB-first data order
- `0xEF` (MSTR): Slave mode selection
- `0xF7` (CPOL): Clock polarity = 0
- `0x04` (CPHA): Clock phase = 1

### Data Reception Protocol
- Interrupt service routine (`ISR(SPI_STC_vect)`) operation
- 3-byte (24-bit) word assembly in `buf[]`
- Explain `process_it` flag and `SSpin` state transitions

### Data Conversion
- Document `concat_convert()` function:
  - Byte concatenation into 32-bit unsigned integer
  - Two's complement conversion for signed 24-bit values
  - Explain magic numbers: `0x800000` (sign bit mask), `0xFF000000` (padding mask)

## 3. Magic Numbers Documentation

For both firmware files, identify and explain ALL magic numbers including:
- **Calibration**: `calib = 10` (averaging window), intercept validation ranges
- **Thresholds**: `dd1_th = 10000`, `deci_th = 150000`, `clamp_val = 0.1`
- **Timing**: `100ms`, `150ms`, `200ms` timeout values
- **State codes**: `-2`, `-1`, `0`, `1` and their semantic meanings
- **Mask values**: `st_mask = 1`, `mid_mask = 0`, `end_mask = -1`, `max_mask = 2`, `data_mask = 3`, `signal_mask = 4`
- **Scaling**: `gradient = 3.575`, division by `100000.0`
- **SPI register masks**: All bitwise operations in `SPI_config()`

## 4. State Machine Documentation

Document the device state transitions:
- State `-2`: Device off/idle
- State `-1`: Post-calibration, awaiting data acquisition
- State `0`: Power-off event detected
- State `1`: Active data acquisition
- Explain `calib_prob` recovery mechanism for improper startup sequences

## Formatting Requirements
- Use clear section headers with `##` and `###`
- Include code snippets where helpful (wrapped in appropriate tags)
- Use tables for pin mappings and state definitions
- Use bullet points for sequential processes
- Ensure all technical terms are defined on first use
- Cross-reference between Application and Calibration sections where workflows overlap