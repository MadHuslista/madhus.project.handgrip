# Documentation Refactor Specification v0.3 — Handgrip Suite

> **Archive status:** Approved implementation plan.
> This document is the Phase 0 archived source-of-truth plan for the documentation refactor.
> It consolidates the approved v0.1 and v0.2 plans and the v0.3 notes about images, PDFs, reader pathways, and deprecated material.

## Summary

- v0.3 supersedes v0.1 and v0.2 while preserving their approved direction: root `README.md` as the first entry point, root `docs/` for system-level documentation, and per-component `README.md` + `docs/` folders for implementation-level documentation.
- The corrected repository/ZIP validates the multi-component workspace: `Handgrip_Firmware`, `RS485_GUI`, `LSL_Bridge`, `LSL_Viewer`, `Handgrip_Calibration`, and `Handgrip_Analysis`.
- The reader-persona and high-level-to-low-level pathway from v0.1 are promoted to governing design principles.
- Images are first-class documentation assets. Existing `Documentation/assets/` photos must be migrated into canonical hardware docs, and the new setup photos must be added before hardware docs are finalized.
- PDFs are classified into fallback references and deprecated material. Acquisition-board manuals, HX711 datasheet, provider offer screencapture, reorganized board manual, and PM58 board manual remain useful. HX710B / old ADV / old MCU material must not appear in canonical docs.
- The prior D2 source-code issue is resolved in firmware source, but the firmware README remains stale and must be updated.
- Two handoff-blocking documentation consistency issues remain: calibration config snapshot paths and primary calibration protocol ambiguity.

## Recommended Action

### Priority 0 — Stabilize source-of-truth contracts before polishing prose

1. Fix stale firmware protocol docs from `D,<seq>,...` to `D2,<seq>,<timestamp_us>,<raw_count>,<current_units>,<status>`.
2. Fix or explicitly document the calibration config snapshot path mismatch from `../RS485_GUI/config.yaml` to `../RS485_GUI/config/config.yaml`.
3. Decide whether `protocol_static_reversible_staircase_v3.yaml` becomes the CLI default, or keep CLI default as legacy while marking v3 as the recommended operator workflow.
4. Add the three new setup photos to the repo before finalizing hardware docs:
   - `pm58_n_handgrip_setup.jpg`
   - `acq_board_n_pm58_n_handgrip_setup.jpg`
   - `force_application_setup.jpg`

### Priority 1 — Build the reader pathway

1. Create root `README.md` as the first screen for all audiences.
2. Create `docs/index.md` as the map from high-level workflows to component internals.
3. Create root workflow docs in this order:
   - physical setup,
   - firmware setup,
   - target-only quickstart,
   - full live-viewer quickstart,
   - calibration workflow,
   - analysis workflow.
4. Create each component `docs/index.md` and component-specific workflow/config/implementation docs.

### Priority 2 — Preserve references without polluting the operator pathway

1. Promote reorganized hardware manuals into `docs/hardware/`.
2. Keep source PDFs under `docs/hardware/references/` with explicit fallback-reference status.
3. Move deprecated HX710B / old ADV / old MCU material out of canonical docs, preferably under `docs/archive/deprecated/old-adv-mcu/`, or remove from the handoff package if source preservation is not required.
4. Add `docs/hardware/assets/README.md` mapping every image to where it is used and what it proves.

### Priority 3 — Make the docs maintainable by students

1. Add configuration reference tables for every config file.
2. Add “how to edit safely” docs for each library.
3. Add a Python source-layout primer targeted at research Python users who are new to industry-style packaging.
4. Add validation commands and checklist-based acceptance tests for the documentation itself.

## Epistemic Status

| Claim / Area | Status | Basis / Next Step |
| --- | ---: | --- |
| Corrected repository structure contains the expected six components | Known | Prior v0.2/v0.3 repository review. |
| Root `README.md` is missing | Known | Root `pyproject.toml` references `README.md`; previous repo review found no root README. |
| Root `docs/` was missing before Phase 0 | Known | Phase 0 materializes it through this archived plan. |
| Existing `Documentation/assets/` contains useful hardware photos | Known | Prior inventory found rear terminal map, AC input, front panel, PM58 label/certificate, and rear-label translation images. |
| Three new setup photos are required | Known | Requested in v0.3 notes. |
| Acquisition-board PDFs and HX711 datasheet should be retained | Known | Requested in v0.3 notes. |
| HX710B / old ADV / old MCU material should be ignored/removed from canonical docs | Known | Requested in v0.3 notes. |
| Firmware source emits canonical D2 payload | Known | Prior v0.2 validation. |
| Firmware README is stale | Known | Prior v0.2 validation. |
| Calibration config snapshot paths are wrong/brittle | Known | Prior v0.2 validation. |
| Canonical calibration protocol is not fully enforced | Known | Prior v0.2 validation. |
| Whether deprecated PDFs should be deleted or archived | Decided in Phase 0 | Default policy: archive under `docs/archive/deprecated/old-adv-mcu/` with strong warning unless the final handoff package intentionally omits them. |

---

# 1. Scope and Inputs

## 1.1 Version lineage

| Plan | Status | Role in v0.3 |
| --- | --- | --- |
| `documentation_refactor_specification.md` | Approved v0.1 | Source for reader personas, high-level-to-low-level pathway, full inventory style, broad gap list, templates, and first directory architecture. |
| `documentation_refactor_specification_updated.md` | Approved v0.2 | Source for corrected-ZIP validation, updated known issues, updated component inventory, and revised execution plan. |
| `docs/archive/documentation-plans/v0.3.md` | Approved v0.3 | Standalone implementation specification for the documentation overhaul. |

## 1.2 Components in scope

| Component | Type | Primary responsibility | Documentation role |
| --- | --- | --- | --- |
| `Handgrip_Firmware` | PlatformIO / Arduino firmware | Read HX711 load-cell data and stream target handgrip data over UART | Firmware setup, build/upload, serial protocol, compile-time configuration, extension guide. |
| `RS485_GUI` | Python / NiceGUI / RS485 acquisition app | Connect to acquisition board, acquire Modbus RTU or Active-Send data, plot/log/publish via ZeroMQ | Reference-board bring-up, live acquisition, IPC contract, logs, GUI config. |
| `LSL_Bridge` | Python / LSL bridge | Publish `HandgripTarget`, `HandgripReference`, and event streams to Lab Streaming Layer | Stream contracts, timestamping policy, target/reference ingestion, CSV logging. |
| `LSL_Viewer` | Python / NiceGUI / LSL viewer | Live visualize target/reference streams, XY correlation, CSV/XDF replay | Operator visualization workflow, troubleshooting visual lag, config options. |
| `Handgrip_Calibration` | Python / CLI calibration workflow | Record calibration sessions, fit target-vs-reference models, generate reports | Calibration protocol, data capture, fitting, outputs, how to apply calibration results. |
| `Handgrip_Analysis` | Python / CLI analysis pipeline | Multi-stage signal characterization and filter-design analysis | Experimental analysis workflow, stage definitions, filter-design interpretation, output catalog. |
| `Documentation` | Legacy documentation/source-reference folder | Historical reports, hardware manuals, PDFs, photos, generated plans | Split into canonical docs, references, and archive/deprecated material. |

---

# 2. Current System Understanding

## 2.1 System purpose

The Handgrip Suite is an end-to-end acquisition, visualization, calibration, and analysis system for a handgrip force-sensing setup. It combines:

1. a target handgrip device based on HX711 load-cell ADC readings streamed by Arduino firmware,
2. a reference acquisition chain based on a PM58 load cell connected to a high-speed RS485 acquisition board,
3. a host-side acquisition GUI for the RS485 board,
4. a Lab Streaming Layer bridge that publishes synchronized target/reference streams,
5. a viewer for live inspection and replay,
6. a calibration workflow for fitting target output to reference force,
7. an analysis workflow for signal-quality characterization and filter selection.

The documentation must support two modes: running the system correctly and modifying it safely.

## 2.2 Canonical physical and data chain

```text
[Handgrip mechanical fixture]
        │
        ├── Target path:
        │     load cell(s) → HX711 → Arduino Nano firmware → UART / USB serial
        │                                      │
        │                                      ▼
        │                              LSL_Bridge target input
        │
        └── Reference path:
              PM58 load cell → high-speed acquisition board → RS485 / USB-RS485
                                                  │
                                                  ▼
                                             RS485_GUI
                                                  │ ZeroMQ IPC
                                                  ▼
                                             LSL_Bridge reference input

LSL_Bridge → Lab Streaming Layer streams → LSL_Viewer / Handgrip_Calibration / recordings
```

## 2.3 Core contracts

| Contract | Canonical value / rule | Why it matters |
| --- | --- | --- |
| Firmware serial schema | `D2,<seq>,<timestamp_us>,<raw_count>,<current_units>,<status>` | LSL bridge parsing and calibration depend on exact field order. |
| Target LSL stream | `HandgripTarget`, type `Force`, D2-derived channels | Calibration and viewer discover the target stream by name/type/schema. |
| Reference LSL stream | `HandgripReference`, type `Force`, RS485-derived channels | Calibration fits target against this reference. |
| RS485 GUI config path | `RS485_GUI/config/config.yaml` | Cross-component config snapshots and operator docs must point to the real file. |
| Reference IPC topic | `rs485.measurement.v1` | LSL bridge consumes RS485 GUI output through this contract. |
| Primary calibration protocol | `Handgrip_Calibration/conf/protocol_static_reversible_staircase_v3.yaml`, unless deliberately changed | Prevents students from running stale/less complete calibration workflows. |
| Hardware reference docs | Acquisition-board manuals, reorganized board manual, PM58 wiring manual, HX711 datasheet | Fallback facts for troubleshooting and extension. |
| Deprecated hardware docs | HX710B / old ADV / old STM32F103 material | Must not confuse the current HX711/Arduino + RS485 board system. |

---

# 3. Reader Personas and Documentation Pathway

## 3.1 Reader personas

### Persona A — Principal investigator / PhD user

**Profile:** Intensive Python user, strong research and neuroscience context, not necessarily trained in industry-standard Python packaging, source-layout architecture, or decoupled application design.

**Primary needs:** understand the full suite, run it safely, interpret calibration/analysis outputs scientifically, and know where students should modify code.

**Best entry points:**

1. `README.md`
2. `docs/start-here.md`
3. `docs/system-overview.md`
4. `docs/workflows/full-live-viewer-quickstart.md`
5. `docs/workflows/handgrip-calibration.md`
6. `docs/workflows/handgrip-analysis.md`

### Persona B — Undergraduate student operator

**Profile:** Needs to run acquisition, validate connections, launch apps in the correct order, capture data, and avoid producing unusable sessions.

**Best entry points:**

1. `README.md` → “I only need to run it”
2. `docs/workflows/physical-setup.md`
3. `docs/workflows/firmware-setup.md`
4. `docs/workflows/target-only-quickstart.md`
5. `docs/workflows/full-live-viewer-quickstart.md`
6. `docs/troubleshooting/index.md`

### Persona C — Undergraduate maintainer/developer

**Profile:** Python-capable, may not know source-layout packaging, Hydra/OmegaConf, Lab Streaming Layer, ZeroMQ, NiceGUI, or decoupled architecture patterns.

**Best entry points:**

1. `docs/development/source-layout-primer.md`
2. `docs/architecture/software-components.md`
3. component `README.md`
4. component `docs/index.md`
5. component `docs/configuration.md`
6. component `docs/implementation.md`

### Persona D — Future maintainer / external collaborator

**Profile:** Needs to understand decisions, constraints, historical reasons, and validation expectations without relying on private conversations.

**Best entry points:**

1. `docs/system-overview.md`
2. `docs/architecture/data-contracts.md`
3. `docs/archive/documentation-plans/v0.3.md`
4. `docs/archive/historical-notes/index.md`
5. `docs/contributing/documentation-style-guide.md`

## 3.2 High-level-to-low-level documentation pathway

Canonical navigation path:

```text
README.md
  └── docs/index.md
        ├── docs/start-here.md
        ├── docs/system-overview.md
        ├── docs/workflows/*.md
        ├── docs/hardware/*.md
        ├── docs/configuration/*.md
        ├── docs/architecture/*.md
        ├── <component>/README.md
        │     └── <component>/docs/index.md
        │           ├── workflow.md
        │           ├── configuration.md
        │           └── implementation.md
        └── docs/archive/*
```

Rule: no canonical workflow should require reading archived development reports first. Archive content can support traceability, but operational docs must stand alone.

---

# 4. Information Inventory

| ID | Status | Detail | Category | Current location | Target location | Related IDs |
| --- | --- | --- | --- | --- | --- | --- |
| INV-001 | Relevant | High | Repo/workspace overview | Missing root `README.md` | `README.md`, `docs/index.md` | INV-002, INV-003 |
| INV-002 | Relevant | High | System architecture | Scattered across prior reports/READMEs | `docs/system-overview.md`, `docs/architecture/software-components.md` | INV-001 |
| INV-003 | Relevant | Medium | Python workspace and source-layout explanation | Implicit in `pyproject.toml` and component layouts | `docs/development/source-layout-primer.md` | INV-001 |
| INV-004 | Relevant | High | Physical wiring photos | `Documentation/assets/` | `docs/hardware/assets/` + `docs/hardware/assets/README.md` | INV-005 |
| INV-005 | Relevant | High | PM58 + acquisition board wiring | `pm58_acquisition_board_manual.md` | `docs/hardware/pm58-acquisition-board.md` | INV-004, INV-006 |
| INV-006 | Relevant | High | Acquisition board full manual | `high_speed_acquisition_instrument_reorganized_manual.md` + PDFs | `docs/hardware/acquisition-board-reference.md`, `docs/hardware/references/acquisition-board/` | INV-005 |
| INV-007 | Relevant | Medium | Provider offer/bundle information | AliExpress screencapture PDF | `docs/hardware/references/acquisition-board/provider-offer-screencapture.pdf` | INV-006 |
| INV-008 | Relevant | High | HX711 datasheet | `hx711_english.pdf` | `docs/hardware/references/hx711/hx711_english.pdf` | INV-009 |
| INV-009 | Relevant | High | Firmware D2 serial protocol | `Handgrip_Firmware/Core/Src/main.cpp`, stale README | `Handgrip_Firmware/README.md`, `Handgrip_Firmware/docs/serial-protocol.md` | INV-008, INV-010 |
| INV-010 | Relevant | High | LSL bridge stream/data contracts | `LSL_Bridge/README.md`, source/configs | `docs/architecture/data-contracts.md`, `LSL_Bridge/docs/*` | INV-009, INV-011 |
| INV-011 | Relevant | High | RS485 GUI config and IPC contract | `RS485_GUI/README.md`, `RS485_GUI/config/config.yaml` | `RS485_GUI/docs/configuration.md`, `docs/architecture/data-contracts.md` | INV-010 |
| INV-012 | Relevant | High | LSL Viewer live/replay workflow | `LSL_Viewer/README.md` | `LSL_Viewer/docs/workflow.md`, `docs/workflows/full-live-viewer-quickstart.md` | INV-010 |
| INV-013 | Relevant | High | Calibration workflow and primary protocol | `Handgrip_Calibration/README.md`, `conf/*.yaml` | `docs/workflows/handgrip-calibration.md`, `Handgrip_Calibration/docs/*` | INV-014 |
| INV-014 | Relevant | High | Calibration outputs and interpretation | generated example reports + calibration docs | `Handgrip_Calibration/docs/results-interpretation.md`, examples | INV-013 |
| INV-015 | Relevant | High | Analysis workflow and stage outputs | `Handgrip_Analysis/README.md`, generated stage docs | `docs/workflows/handgrip-analysis.md`, `Handgrip_Analysis/docs/*` | INV-014 |
| INV-016 | Relevant | Medium | Development/history notes | `Binnacle.md`, legacy reports | `docs/archive/historical-notes/` | INV-001 |
| INV-017 | Outdated | Medium | HX710B old ADC docs | `Documentation/` PDFs | `docs/archive/deprecated/old-adv-mcu/` or omit from handoff | INV-008 |
| INV-018 | Outdated | Medium | Old STM32F103/ADV material | `Documentation/` PDFs/assets | `docs/archive/deprecated/old-adv-mcu/` or omit from handoff | INV-009 |

---

# 5. Ideal Final Directory Tree

```text
.
├── README.md
├── docs/
│   ├── index.md
│   ├── start-here.md
│   ├── system-overview.md
│   ├── architecture/
│   │   ├── index.md
│   │   ├── data-contracts.md
│   │   ├── software-components.md
│   │   └── timing-and-synchronization.md
│   ├── workflows/
│   │   ├── index.md
│   │   ├── physical-setup.md
│   │   ├── firmware-setup.md
│   │   ├── target-only-quickstart.md
│   │   ├── reference-only-quickstart.md
│   │   ├── full-live-viewer-quickstart.md
│   │   ├── handgrip-calibration.md
│   │   └── handgrip-analysis.md
│   ├── hardware/
│   │   ├── index.md
│   │   ├── acquisition-board-reference.md
│   │   ├── pm58-acquisition-board.md
│   │   ├── force-fixture-setup.md
│   │   ├── assets/
│   │   │   ├── README.md
│   │   │   ├── rear-terminal-map_full-feature-selection.jpg
│   │   │   ├── ac-input-sticker_close-up.jpg
│   │   │   ├── front-panel_buttons_indicators.jpg
│   │   │   ├── pm58-load-cell_label.jpg
│   │   │   ├── pm58-certificate_wire-colors.jpg
│   │   │   ├── rear-label_google-lens-translation.jpg
│   │   │   ├── pm58_n_handgrip_setup.jpg
│   │   │   ├── acq_board_n_pm58_n_handgrip_setup.jpg
│   │   │   └── force_application_setup.jpg
│   │   └── references/
│   │       ├── acquisition-board/
│   │       └── hx711/
│   ├── configuration/
│   │   ├── index.md
│   │   └── cross-component-config-map.md
│   ├── development/
│   │   ├── index.md
│   │   ├── source-layout-primer.md
│   │   └── how-to-extend-safely.md
│   ├── troubleshooting/
│   │   ├── index.md
│   │   ├── no-serial-data.md
│   │   ├── no-rs485-data.md
│   │   ├── no-lsl-streams.md
│   │   └── viewer-lag-or-desync.md
│   ├── examples/
│   │   ├── calibration-session/
│   │   └── analysis-output/
│   ├── contributing/
│   │   ├── index.md
│   │   └── documentation-style-guide.md
│   └── archive/
│       ├── documentation-plans/
│       │   └── v0.3.md
│       ├── historical-notes/
│       └── deprecated/
│           └── old-adv-mcu/
│               └── README.md
├── Handgrip_Firmware/
│   ├── README.md
│   └── docs/
│       ├── index.md
│       ├── firmware-setup.md
│       ├── serial-protocol.md
│       ├── configuration.md
│       └── implementation.md
├── RS485_GUI/
│   └── docs/
├── LSL_Bridge/
│   └── docs/
├── LSL_Viewer/
│   └── docs/
├── Handgrip_Calibration/
│   └── docs/
└── Handgrip_Analysis/
    └── docs/
```

---

# 6. Execution Plan

## Phase 0 — Prepare documentation branch and freeze decisions

### Step 0.1 — Documentation branch

- **Do:** Use `docs-deep` as the parent branch for all plan execution.
- **Actual Phase 0 decision:** Commit Phase 0 artifacts directly to `docs-deep` unless a later PR workflow requires a child branch.
- **Expected result:** Documentation execution history is isolated from `main`.

### Step 0.2 — Archive v0.3 specification

- **Do:** Save this plan as `docs/archive/documentation-plans/v0.3.md`.
- **Expected result:** Future maintainers can see why docs were reorganized.

### Step 0.3 — Decide deprecated-source handling

- **Decision:** Archive old HX710B / old ADV / old MCU material under `docs/archive/deprecated/old-adv-mcu/` when preservation is required; otherwise omit from the final handoff package.
- **Canonical rule:** No root README, root workflow, component README, or component docs page may rely on deprecated material.

## Phase 1 — Create documentation skeleton

Create root docs folders and component docs folders. Add `index.md` files so every docs folder is navigable.

## Phase 2 — Root entrypoint and reader pathway

Create root `README.md`, `docs/start-here.md`, and `docs/system-overview.md`.

## Phase 3 — Hardware, images, and PDFs

Migrate useful images, add the three new setup photos, create hardware assets manifest, promote useful hardware manuals, keep source PDFs as fallback references, and archive/omit deprecated HX710B/old MCU material.

## Phase 4 — Workflows

Create physical setup, firmware setup, target-only quickstart, reference-only quickstart, full live-viewer quickstart, calibration workflow, and analysis workflow.

## Phase 5 — Component docs

For each component, create `docs/index.md`, workflow, configuration reference, implementation guide, troubleshooting notes, and output/reference docs where relevant.

## Phase 6 — Configuration references

Create root cross-component config map and per-component config tables with key, type, default, allowed values, operational impact, when to change, and failure risk.

## Phase 7 — Fix known documentation/code-doc mismatches

Fix firmware D2 protocol docs, RS485 GUI config snapshot paths, and calibration protocol default ambiguity.

## Phase 8 — Validation and handoff dry-run

Run path/link validation, stale-reference checks, and the full student-operator dry run.

---

# 7. Style Guide

## Required document shape

Every maintained document should use summary-first hierarchical detail:

```markdown
# <Title>

## Summary

## Audience

## Status

## Recommended Action / Workflow

## Details

## Validation

## Troubleshooting

## References
```

## Workflow step format

```markdown
### Step N — <Action>

- **Do:** <Concrete action>
- **Command / file:** `<path_or_command>`
- **Expected result:** <Success signal>
- **Failure signal:** <What indicates a problem>
- **Next branch:** <What to do next>
```

## Configuration reference format

```markdown
| Key | Type | Default | Allowed values | Operational impact | When to change | Failure risk |
| --- | --- | --- | --- | --- | --- | --- |
```

## Archive banner format

```markdown
> **Archive status:** Deprecated / historical.
> This file is not part of the current canonical Handgrip Suite workflow.
> Do not use it for current wiring, firmware, calibration, or analysis unless explicitly instructed.
```

---

# 8. Updated High-Priority Known Issues

| ID | Issue | Severity | Current status | Required action |
| --- | --- | ---: | --- | --- |
| KI-1 | Root `README.md` missing | High | Still true | Create root README. |
| KI-2 | Root `docs/` missing | High | Phase 0 starts it | Complete docs skeleton. |
| KI-3 | Firmware README stale protocol | High | Source fixed; README stale | Update firmware docs to D2 schema. |
| KI-4 | Calibration config snapshot path mismatch | High | Still true | Change to `../RS485_GUI/config/config.yaml`. |
| KI-5 | Calibration protocol default ambiguity | High | Still true | Make v3 CLI default or enforce explicit v3 in docs. |
| KI-6 | New setup images absent | Medium | Missing | Add three requested images to `docs/hardware/assets/`. |
| KI-7 | Deprecated HX710B/old MCU docs mixed with useful docs | Medium | Still present | Remove from canonical; archive or delete. |
| KI-8 | No complete config reference per component | Medium | Still true | Author config docs. |
| KI-9 | Generated outputs mixed with maintained docs | Medium | Still true | Curate examples or mark as generated. |
| KI-10 | Source-layout not explained for research handoff audience | Medium | Still true | Add development primer and component maps. |

---

# 9. Validation Plan

| Check | Method | Expected result | Failure meaning |
| --- | --- | --- | --- |
| Root README exists | `test -f README.md` | File exists and links to docs | No canonical entry point. |
| Root docs index exists | `test -f docs/index.md` | File exists | Docs tree not navigable. |
| Component docs indexes exist | loop over components | All `docs/index.md` exist | Component docs incomplete. |
| Useful images exist | `find docs/hardware/assets` | All required images present | Hardware docs cannot be visual/reproducible. |
| New setup photos exist | `test -f` for three filenames | All present | Force-fixture docs incomplete. |
| Acquisition-board PDFs preserved | `find docs/hardware/references/acquisition-board` | Source PDFs present | Reference fallback missing. |
| HX711 datasheet preserved | `test -f docs/hardware/references/hx711/hx711_english.pdf` | Present | Firmware ADC reference missing. |
| Deprecated docs not linked in canonical docs | `rg HX710B README.md docs --glob '!docs/archive/**'` | No hits except explicit deprecation notes | Old hardware may confuse users. |
| Firmware D2 schema documented | `rg "D2,<seq>,<timestamp_us>,<raw_count>,<current_units>,<status>"` | Hits in protocol docs | Parser contract not documented. |
| Legacy D schema removed | `rg "D,<seq>|value_gr" README.md docs Handgrip_Firmware` | No stale hits | Firmware docs still misleading. |
| RS485 GUI config path fixed | `rg "../RS485_GUI/config.yaml" Handgrip_Calibration/conf` | No hits | Config snapshots may fail. |
| v3 calibration protocol canonical | docs + CLI check | Docs and CLI agree | Operators may run wrong protocol. |

## Handoff dry-run sequence

1. Read root `README.md` only.
2. Follow `docs/workflows/physical-setup.md` and validate hardware visually.
3. Follow `docs/workflows/firmware-setup.md` and confirm D2 serial frames.
4. Follow `docs/workflows/reference-only-quickstart.md` and confirm RS485 GUI data.
5. Follow `docs/workflows/full-live-viewer-quickstart.md` and confirm both streams live.
6. Follow `docs/workflows/handgrip-calibration.md` with a smoke protocol or v3 protocol.
7. Follow `docs/workflows/handgrip-analysis.md` on known input data.
8. Ask a maintainer to modify one non-critical config and revert it using docs.

Acceptance condition: a student can complete steps 1–7 without private context.

---

# 10. Definition of Done

The documentation refactor is complete when:

1. Root `README.md` exists and is the obvious entry point.
2. Root `docs/index.md` maps all reader pathways.
3. Every component has `README.md`, `docs/index.md`, workflow doc, configuration reference, and implementation doc.
4. Hardware docs use canonical image assets, including the three new setup photos.
5. Acquisition-board PDFs, HX711 datasheet, and provider offer are preserved as fallback references.
6. HX710B / old ADV / old STM32F103 material is absent from canonical docs.
7. Firmware docs consistently use D2 schema.
8. Calibration docs and CLI defaults agree on the primary protocol, or docs explicitly force v3 with `--config`.
9. Calibration config snapshot paths point to real files.
10. Configuration values are documented with impact/range/failure risk.
11. Generated outputs are labeled as examples or kept outside maintained docs.
12. Link/path validation passes.
13. A student operator can follow the documented workflow from physical setup to live viewer and calibration without private context.

---

# Appendix A — Immediate Patch Checklist

```bash
# 1. Create docs skeleton.
mkdir -p docs/{architecture,workflows,hardware/assets,hardware/references/acquisition-board,hardware/references/hx711,configuration,development,troubleshooting,examples/calibration-session,examples/analysis-output,contributing,archive/documentation-plans,archive/historical-notes,archive/deprecated/old-adv-mcu}
for d in Handgrip_Firmware RS485_GUI LSL_Bridge LSL_Viewer Handgrip_Calibration Handgrip_Analysis; do mkdir -p "$d/docs"; done

# 2. Find stale firmware schema docs.
rg "D,<seq>|value_gr" Handgrip_Firmware README.md docs Documentation || true

# 3. Find stale RS485 GUI config paths.
rg "../RS485_GUI/config.yaml" Handgrip_Calibration/conf

# 4. Find old protocol defaults.
rg "protocol_static_staircase.yaml" Handgrip_Calibration Documentation docs || true

# 5. Find deprecated old hardware mentions in canonical docs after migration.
rg "HX710B|stm32f103|Hacer bascula" README.md docs --glob '!docs/archive/**' || true
```
